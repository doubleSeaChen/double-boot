<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.notice.dao.NoticeRecordDao">

	<select id="get" resultType="com.notice.domain.NoticeRecordDO">
		select `id`,`notify_id`,`user_id`,`is_read`,`read_date` from notice_record where id = #{value}
	</select>

	<select id="list" resultType="com.notice.domain.NoticeRecordDO">
		select `id`,`notify_id`,`user_id`,`is_read`,`read_date` from notice_record
        <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="notifyId != null and notifyId != ''"> and notify_id = #{notifyId} </if>
		  		  <if test="userId != null and userId != ''"> and user_id = #{userId} </if>
		  		  <if test="isRead != null and isRead != ''"> and is_read = #{isRead} </if>
		  		  <if test="readDate != null and readDate != ''"> and read_date = #{readDate} </if>
		  		</where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="count" resultType="int">
		select count(*) from notice_record
		 <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="notifyId != null and notifyId != ''"> and notify_id = #{notifyId} </if>
		  		  <if test="userId != null and userId != ''"> and user_id = #{userId} </if>
		  		  <if test="isRead != null and isRead != ''"> and is_read = #{isRead} </if>
		  		  <if test="readDate != null and readDate != ''"> and read_date = #{readDate} </if>
		  		</where>
	</select>
	 
	<insert id="save" parameterType="com.notice.domain.NoticeRecordDO">
		insert into notice_record
		(
			`id`, 
			`notify_id`, 
			`user_id`, 
			`is_read`, 
			`read_date`
		)
		values
		(
			#{id}, 
			#{notifyId}, 
			#{userId}, 
			#{isRead}, 
			#{readDate}
		)
	</insert>
	 
	<update id="update" parameterType="com.notice.domain.NoticeRecordDO">
		update notice_record 
		<set>
			<if test="notifyId != null">`notify_id` = #{notifyId}, </if>
			<if test="userId != null">`user_id` = #{userId}, </if>
			<if test="isRead != null">`is_read` = #{isRead}, </if>
			<if test="readDate != null">`read_date` = #{readDate}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="remove">
		delete from notice_record where id = #{value}
	</delete>
	
	<delete id="batchRemove">
		delete from notice_record where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>